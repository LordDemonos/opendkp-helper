# DISABLED: Auto-increment on push has been disabled
# Version now only increments when creating a GitHub release
# See build-release.yml for release-time version incrementing

name: Auto-Increment Version (DISABLED)

on:
  workflow_dispatch: # Only runs if manually triggered (for emergency use)
  # push: # DISABLED - version only increments on release now
  #   branches:
  #     - master
  #     - main

jobs:
  increment-version:
    name: Increment Patch Version (DISABLED)
    runs-on: ubuntu-latest
    if: false # Always skip - workflow is disabled
    
    steps:
      - name: Workflow Disabled
        run: |
          echo "⚠️  This workflow is disabled."
          echo "Version now only increments when creating a GitHub release."
          echo "See build-release.yml for release-time version incrementing."
